pipeline {
   agent any
  stages {
    stage("build") {
      steps {
        echo 'build application ....'
      }
    stage("build") {
      steps {
        echo 'build application ....'
      }
    }
      }
  }
}
